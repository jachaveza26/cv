# CV Site Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build and publish `https://andres.streamlite.ca`, a single-page English CV site for Andrés Chávez generated from one JSON file, with a PDF produced from the same source, served by Caddy on Railway.

**Architecture:** `content/cv.json` is validated against a small JSON Schema by a dependency-free Node build that renders `dist/index.html` + `styles.css` + assets. Playwright (dev dependency only) prints the page to `dist/Andres-Chavez-CV.pdf` and renders the Open Graph image. A two-stage Dockerfile (Playwright image builds, `caddy:2-alpine` serves) is what Railway deploys; GitHub Actions runs the same build plus tests, html-validate and lychee as a quality signal.

**Tech Stack:** Node 22 (ESM, `node:test`), Playwright 1.63.0, html-validate 11.15.0, Caddy 2, Docker, Railway, GitHub Actions, lychee.

**Spec:** `cv/docs/superpowers/specs/2026-09-10-cv-site-design.md`

## Global Constraints

- Working directory for every task: `/Users/achavez/GitHub/portfolio-jachaveza26/cv` (already `git init -b main`, two commits).
- **No runtime dependencies.** `src/build.mjs`, `src/render.mjs`, `src/validate.mjs` import only `node:*` modules. `devDependencies` are exactly `playwright` and `html-validate`.
- **Content rules:** nothing that is not in `../Resume_JAndresChavez.pdf` or in `github.com/jachaveza26/portfolio`. No phone number anywhere. Name shown as "Andrés Chávez". Email `andres@streamlite.ca`. Camosun certificate completed `2025-03-18`. Streamlite entry starts `2025-01`, end `null`.
- **Git rules:** commit messages are conventional (`feat:`, `test:`, `docs:`, `chore:`, `ci:`), **no `Co-Authored-By` trailer**, no force push, no history rewriting. Nothing is pushed or created on GitHub or Railway without Andrés's explicit approval (Task 7 only). `../Resume_JAndresChavez.pdf` and `../COMO-PUBLICAR.md` are outside the repo and must never be copied in.
- `dist/` and `node_modules/` are gitignored (already in `.gitignore`).
- Playwright version `1.63.0` exactly, and the Docker build image tag `mcr.microsoft.com/playwright:v1.63.0-jammy` must match it.
- Lychee accept list, same as the portfolio: `200,206,403,429,999`.
- Site URL constant: `https://andres.streamlite.ca`.

---

## File structure

| File | Responsibility |
|---|---|
| `package.json`, `package-lock.json` | scripts and the two dev dependencies |
| `content/cv.schema.json` | shape of the content; single place that says what is required |
| `content/cv.json` | all content; the only file Andrés edits day to day |
| `src/validate.mjs` | `validate(value, schema, path) → string[]` minimal JSON Schema subset |
| `src/render.mjs` | pure functions: `render(cv, {updated}) → html string`, plus `esc`, `fmtMonth`, `slug` |
| `src/build.mjs` | I/O: read, validate, render, write `dist/`, copy css + assets; CLI entry |
| `src/styles.css` | screen + print styles |
| `assets/andres-chavez.jpg`, `assets/andres-chavez-400.webp`, `assets/og-image.png` | images, committed |
| `scripts/pdf.mjs` | Playwright: `dist/index.html` → `dist/Andres-Chavez-CV.pdf`, page-count check |
| `scripts/og.mjs` | Playwright: renders `assets/og-image.png` 1200×630 (run once, committed) |
| `test/validate.test.mjs` | unit tests for the validator |
| `test/build.test.mjs` | build output tests |
| `.htmlvalidate.json` | html-validate config |
| `Dockerfile`, `Caddyfile`, `railway.json`, `.dockerignore` | deploy artifact |
| `.github/workflows/checks.yml` | quality gate |
| `README.md` | what, how to build, badge |

---

### Task 1: Project scaffold, schema validator and content

**Files:**
- Create: `package.json`, `content/cv.schema.json`, `content/cv.json`, `src/validate.mjs`, `test/validate.test.mjs`

**Interfaces:**
- Produces: `validate(value, schema, path = "$") → string[]` (empty array = valid). Supports `type` (string or array, incl. `"null"`), `required`, `properties`, `additionalProperties: false`, `items`, `minItems`, `enum`, `pattern`, `minLength`.
- Produces: `content/cv.json` with the exact shape below, consumed by Task 2.

- [ ] **Step 1: Create `package.json` and install dev dependencies**

```json
{
  "name": "andres-cv",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node src/build.mjs",
    "test": "node --test test/",
    "pdf": "node scripts/pdf.mjs",
    "og": "node scripts/og.mjs",
    "validate:html": "html-validate dist/index.html",
    "serve": "npx --yes serve dist"
  },
  "devDependencies": {
    "html-validate": "11.15.0",
    "playwright": "1.63.0"
  }
}
```

Run: `npm install` (creates `package-lock.json`, needed by `npm ci` in Docker and CI).
Expected: `node_modules/` created, no errors. Do **not** run `npx playwright install` yet; Task 4 does it.

- [ ] **Step 2: Write the failing validator tests**

`test/validate.test.mjs`:

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { validate } from "../src/validate.mjs";

const schema = {
  type: "object",
  required: ["name", "start", "tags"],
  additionalProperties: false,
  properties: {
    name: { type: "string", minLength: 1 },
    start: { type: "string", pattern: "^\\d{4}-\\d{2}$" },
    end: { type: ["string", "null"], pattern: "^\\d{4}-\\d{2}$" },
    level: { type: "string", enum: ["a", "b"] },
    tags: { type: "array", minItems: 1, items: { type: "string" } },
  },
};

test("valid object returns no errors", () => {
  assert.deepEqual(validate({ name: "x", start: "2025-01", end: null, tags: ["t"] }, schema), []);
});

test("missing required property is reported with its path", () => {
  const errors = validate({ name: "x", tags: ["t"] }, schema);
  assert.deepEqual(errors, ["$.start: required"]);
});

test("wrong type, bad pattern, bad enum, empty array, extra property are all reported", () => {
  const errors = validate({ name: 3, start: "2025", end: "x", level: "c", tags: [], extra: 1 }, schema);
  assert.ok(errors.some((e) => e.startsWith("$.name: expected string")));
  assert.ok(errors.includes("$.start: does not match ^\\d{4}-\\d{2}$"));
  assert.ok(errors.includes("$.end: does not match ^\\d{4}-\\d{2}$"));
  assert.ok(errors.includes("$.level: not one of a, b"));
  assert.ok(errors.includes("$.tags: fewer than 1 items"));
  assert.ok(errors.includes("$.extra: unexpected property"));
});

test("array items are validated with their index in the path", () => {
  const errors = validate({ name: "x", start: "2025-01", tags: ["ok", 5] }, schema);
  assert.deepEqual(errors, ["$.tags[1]: expected string, got number"]);
});
```

- [ ] **Step 3: Run the tests to verify they fail**

Run: `node --test test/validate.test.mjs`
Expected: FAIL, `Cannot find module '../src/validate.mjs'`.

- [ ] **Step 4: Implement `src/validate.mjs`**

```js
// Minimal JSON Schema subset validator. No dependencies on purpose:
// the build must run with nothing but Node.
export function validate(value, schema, path = "$") {
  const errors = [];
  const types = Array.isArray(schema.type) ? schema.type : schema.type ? [schema.type] : [];
  const actual = value === null ? "null" : Array.isArray(value) ? "array" : typeof value;
  if (types.length && !types.includes(actual)) {
    errors.push(`${path}: expected ${types.join("|")}, got ${actual}`);
    return errors;
  }
  if (schema.enum && !schema.enum.includes(value)) errors.push(`${path}: not one of ${schema.enum.join(", ")}`);
  if (typeof value === "string") {
    if (schema.pattern && !new RegExp(schema.pattern).test(value)) errors.push(`${path}: does not match ${schema.pattern}`);
    if (schema.minLength !== undefined && value.length < schema.minLength) errors.push(`${path}: shorter than ${schema.minLength}`);
  }
  if (actual === "object") {
    for (const key of schema.required ?? []) if (!(key in value)) errors.push(`${path}.${key}: required`);
    const props = schema.properties ?? {};
    for (const [key, sub] of Object.entries(props)) if (key in value) errors.push(...validate(value[key], sub, `${path}.${key}`));
    if (schema.additionalProperties === false) for (const key of Object.keys(value)) if (!(key in props)) errors.push(`${path}.${key}: unexpected property`);
  }
  if (actual === "array") {
    if (schema.minItems !== undefined && value.length < schema.minItems) errors.push(`${path}: fewer than ${schema.minItems} items`);
    if (schema.items) value.forEach((item, i) => errors.push(...validate(item, schema.items, `${path}[${i}]`)));
  }
  return errors;
}
```

- [ ] **Step 5: Run the tests to verify they pass**

Run: `node --test test/validate.test.mjs`
Expected: 4 passing.

- [ ] **Step 6: Write `content/cv.schema.json`**

```json
{
  "type": "object",
  "required": ["identity", "profile", "experience", "projects", "skills", "education", "certifications", "languages"],
  "additionalProperties": false,
  "properties": {
    "identity": {
      "type": "object",
      "required": ["name", "title", "tagline", "location", "email", "linkedin", "github", "portfolio", "site", "photo"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "title": { "type": "string", "minLength": 1 },
        "tagline": { "type": "string", "minLength": 1 },
        "location": { "type": "string", "minLength": 1 },
        "email": { "type": "string", "pattern": "^[^@\\s]+@[^@\\s]+\\.[a-z]{2,}$" },
        "linkedin": { "type": "string", "pattern": "^https://www\\.linkedin\\.com/in/" },
        "github": { "type": "string", "pattern": "^https://github\\.com/" },
        "portfolio": { "type": "string", "pattern": "^https://github\\.com/jachaveza26/portfolio$" },
        "site": { "type": "string", "pattern": "^https://" },
        "photo": { "type": "string", "pattern": "\\.(webp|jpg|png)$" }
      }
    },
    "profile": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 } },
    "experience": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["role", "org", "location", "start", "end", "summary", "bullets", "links", "compact"],
        "additionalProperties": false,
        "properties": {
          "role": { "type": "string", "minLength": 1 },
          "org": { "type": "string", "minLength": 1 },
          "location": { "type": "string", "minLength": 1 },
          "start": { "type": "string", "pattern": "^\\d{4}-\\d{2}$" },
          "end": { "type": ["string", "null"], "pattern": "^\\d{4}-\\d{2}$" },
          "summary": { "type": "string" },
          "bullets": { "type": "array", "items": { "type": "string", "minLength": 1 } },
          "links": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["label", "url"],
              "additionalProperties": false,
              "properties": { "label": { "type": "string", "minLength": 1 }, "url": { "type": "string", "pattern": "^https://" } }
            }
          },
          "compact": { "type": "boolean" }
        }
      }
    },
    "projects": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["name", "oneLiner", "stack", "status", "url"],
        "additionalProperties": false,
        "properties": {
          "name": { "type": "string", "minLength": 1 },
          "oneLiner": { "type": "string", "minLength": 1 },
          "stack": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 } },
          "status": { "type": "string", "minLength": 1 },
          "url": { "type": "string", "pattern": "^https://github\\.com/jachaveza26/portfolio/blob/main/projects/[a-z0-9-]+\\.md$" }
        }
      }
    },
    "skills": {
      "type": "object",
      "required": ["hard", "soft"],
      "additionalProperties": false,
      "properties": {
        "hard": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": ["group", "items"],
            "additionalProperties": false,
            "properties": {
              "group": { "type": "string", "minLength": 1 },
              "items": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 } }
            }
          }
        },
        "soft": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": ["name", "evidence"],
            "additionalProperties": false,
            "properties": { "name": { "type": "string", "minLength": 1 }, "evidence": { "type": "string", "minLength": 1 } }
          }
        }
      }
    },
    "education": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["credential", "institution", "country", "year"],
        "additionalProperties": false,
        "properties": {
          "credential": { "type": "string", "minLength": 1 },
          "institution": { "type": "string", "minLength": 1 },
          "country": { "type": "string", "minLength": 1 },
          "year": { "type": "string", "pattern": "^\\d{4}$" }
        }
      }
    },
    "certifications": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["credential", "institution", "country", "completed"],
        "additionalProperties": false,
        "properties": {
          "credential": { "type": "string", "minLength": 1 },
          "institution": { "type": "string", "minLength": 1 },
          "country": { "type": "string", "minLength": 1 },
          "completed": { "type": "string", "pattern": "^\\d{4}(-\\d{2}-\\d{2})?$" }
        }
      }
    },
    "languages": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["language", "level"],
        "additionalProperties": false,
        "properties": { "language": { "type": "string", "minLength": 1 }, "level": { "type": "string", "minLength": 1 } }
      }
    }
  }
}
```

- [ ] **Step 7: Write `content/cv.json`**

Every line below comes from the CV PDF or the portfolio. This is the draft Andrés reviews in Task 7 before anything is published; wording changes he asks for are applied here and only here.

```json
{
  "identity": {
    "name": "Andrés Chávez",
    "title": "AI & Automation Engineer",
    "tagline": "I build LLM systems that run in production — with human approval gates, fact-checking, and the boring operational plumbing that keeps them honest.",
    "location": "Chilliwack, BC, Canada",
    "email": "andres@streamlite.ca",
    "linkedin": "https://www.linkedin.com/in/jachaveza/",
    "github": "https://github.com/jachaveza26",
    "portfolio": "https://github.com/jachaveza26/portfolio",
    "site": "https://andres.streamlite.ca",
    "photo": "andres-chavez-400.webp"
  },
  "profile": [
    "AI & Automation Engineer and founder of Streamlite Technologies, building LLM systems that run in production for clients in Canada, the US and Mexico: integrations with APIs that fight back, human approval gates so nothing publishes unreviewed, and the operational plumbing that keeps both honest.",
    "Six years of data analytics underneath — Power BI, DAX, SQL — which is why I care what a number means before I automate it. Before that, a decade of systems work in Mexico: software engineering, SAP ABAP, web applications and end-user support.",
    "I own the whole line: discovery with the client, architecture, build, deploy, and the 2 a.m. alert. Bilingual in Spanish and English. Open to AI / Automation Engineer and Forward Deployed Engineer roles."
  ],
  "experience": [
    {
      "role": "Founder & AI/Automation Engineer",
      "org": "Streamlite Technologies",
      "location": "Chilliwack, BC",
      "start": "2025-01",
      "end": null,
      "summary": "AI automation consultancy for small and mid-size businesses across Canada, the US and Mexico. Sole engineer on every engagement: discovery, architecture, build, deploy, billing and on-call.",
      "bullets": [
        "Built and operate GBP Manager, a multi-tenant SaaS that audits Google Business Profiles and drafts review replies and posts with an LLM; nothing publishes without a per-item human approval record. Google API production access approved after two rejections. Audited my own stack six weeks in and closed four security issues, including approval links being auto-approved by corporate email scanners.",
        "Automated Canadian immigration PDF forms from one canonical intake schema using Claude tool use and XFA XML manipulation; in production for an immigration consultancy.",
        "Delivered ScopeForge, a bid-intelligence platform for a commercial millwork subcontractor: AI scope extraction from bid packages, BuildingConnected sync, DOCX proposal generation and an automated follow-up sequence.",
        "Own the BI and compliance stack for a Mexican POS distributor: a ~30M-row Power BI model with incremental refresh, a redesigned daily alerting system, and a PLD/AML transactional monitoring layer with fraud detectors, per-merchant thresholds and an audited retention workflow.",
        "Shipped Relay, social and local-SEO automation for a paying client, on the Meta Graph API at Standard Access with per-client isolated deployments.",
        "Built a Microsoft licence drift detector as a paid-evaluation prototype for a managed service provider: 100% recall and zero false positives against a ground-truth dataset."
      ],
      "links": [
        { "label": "GBP Manager", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/gbp-manager.md" },
        { "label": "IRCC Form Automation", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/ircc-form-automation.md" },
        { "label": "ScopeForge", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/scopeforge-mci-bids.md" },
        { "label": "Prosepago", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/prosepago-bi.md" },
        { "label": "Relay", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/relay.md" },
        { "label": "Licence Reconciliation", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/licence-reconciliation.md" }
      ],
      "compact": false
    },
    {
      "role": "Computer System Analyst",
      "org": "Fernando Torres Immigration",
      "location": "Vancouver, BC",
      "start": "2022-04",
      "end": "2024-12",
      "summary": "",
      "bullets": [
        "Implemented and maintained the CRM system (Pipedrive), ensuring data accuracy and consistency.",
        "Led website functionality updates and content management for immigration programs and services.",
        "Developed landing pages to optimize lead generation and conversion.",
        "Analyzed data to generate Power BI reports and insights for decision-making on immigration client needs."
      ],
      "links": [],
      "compact": false
    },
    {
      "role": "Data Analyst",
      "org": "Agile Thought Digital Solutions",
      "location": "Mexico",
      "start": "2021-08",
      "end": "2022-09",
      "summary": "",
      "bullets": [
        "Designed and developed Power BI dashboards on sales performance and customer behaviour for the retail industry.",
        "Implemented advanced data visualizations in Power BI for comprehensive analysis.",
        "Ran training sessions on effective use of Power BI for the data analytics team."
      ],
      "links": [],
      "compact": false
    },
    {
      "role": "Data Analyst",
      "org": "Conduent de México",
      "location": "Mexico",
      "start": "2021-04",
      "end": "2022-05",
      "summary": "",
      "bullets": [
        "Wrote and optimized SQL statements for more efficient data analysis.",
        "Designed customized data collection models for visualization tasks.",
        "Created data analysis tools for easy querying and insight generation.",
        "Implemented advanced data visualizations in Power BI dashboards."
      ],
      "links": [],
      "compact": false
    },
    {
      "role": "Business Data Analyst",
      "org": "PSM Payment Services",
      "location": "Mexico",
      "start": "2020-09",
      "end": "2021-08",
      "summary": "",
      "bullets": [
        "Implemented a CRM (ActiveCampaign) and trained employees on it for improved data management.",
        "Implemented advanced data visualizations in Power BI dashboards."
      ],
      "links": [],
      "compact": false
    },
    {
      "role": "IT Support",
      "org": "ZMBDi Management & Consulting",
      "location": "Mexico",
      "start": "2020-09",
      "end": "2021-09",
      "summary": "",
      "bullets": [
        "Designed, implemented and monitored web pages and plugins for continuous improvement.",
        "Implemented a CRM and trained employees on it.",
        "Provided front-end development and website maintenance training.",
        "Used market research strategies to improve campaign planning."
      ],
      "links": [],
      "compact": false
    },
    {
      "role": "Computer System Analyst",
      "org": "Desarrollos y Servicios Computacionales",
      "location": "Mexico",
      "start": "2013-05",
      "end": "2020-08",
      "summary": "End-user troubleshooting and desktop support across multiple operating systems; software and hardware solutions for business clients, mainly in the restaurant industry; website design, implementation and monitoring.",
      "bullets": [],
      "links": [],
      "compact": true
    },
    {
      "role": "Software Engineer",
      "org": "LANIX",
      "location": "Mexico",
      "start": "2009-07",
      "end": "2013-05",
      "summary": "Server software implementation and virtual machine management; development and maintenance of web applications and database design; SAP ABAP development (Z tables, Smart Forms, user exits, ALV reports).",
      "bullets": [],
      "links": [],
      "compact": true
    }
  ],
  "projects": [
    { "name": "GBP Manager", "oneLiner": "Multi-tenant AI SaaS that audits Google Business Profiles, drafts review replies and posts — every publish behind a human approval record.", "stack": ["Next.js", "Python", "Supabase", "Stripe", "Railway"], "status": "Production, pre-traction", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/gbp-manager.md" },
    { "name": "IRCC Form Automation", "oneLiner": "Fills Canadian immigration PDFs from a canonical intake schema using LLM tool use and XFA XML manipulation.", "stack": ["FastAPI", "Claude Tool Use", "XFA XML"], "status": "Production", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/ircc-form-automation.md" },
    { "name": "ScopeForge / MCI Bids", "oneLiner": "Bid-intelligence platform for commercial construction subcontractors: AI scope extraction, proposal generation, pipeline tracking.", "stack": ["Python", "Railway", "BuildingConnected API"], "status": "Production, 1 client", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/scopeforge-mci-bids.md" },
    { "name": "Relay", "oneLiner": "Multi-tenant social and local-SEO automation, deployed per client on isolated branches.", "stack": ["TypeScript", "Supabase", "Meta Graph API", "DataForSEO"], "status": "Production, paying client", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/relay.md" },
    { "name": "Prosepago", "oneLiner": "~30M-row Power BI model for a Mexican POS distributor, plus PLD/AML transactional monitoring and open-data prospecting.", "stack": ["Power BI", "DAX", "Python", "PostgreSQL"], "status": "Production", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/prosepago-bi.md" },
    { "name": "Eden Estimator", "oneLiner": "The repo where my engineering practices live: Vitest suite, Docker, Caddy, pnpm monorepo, sliced delivery.", "stack": ["TypeScript", "Vitest", "Docker", "Supabase"], "status": "In development", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/eden-estimator.md" },
    { "name": "Licence Reconciliation", "oneLiner": "Microsoft licence drift detector built as a paid-evaluation prototype — 100% recall, zero false positives on a ground-truth set.", "stack": ["Python", "FastAPI", "Microsoft Graph"], "status": "Prototype, delivered", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/licence-reconciliation.md" },
    { "name": "Voice Agents", "oneLiner": "Retell and VAPI voice agents embedded in a Next.js product surface.", "stack": ["Next.js", "Retell AI", "VAPI"], "status": "Production", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/voice-agents.md" },
    { "name": "CFSF Loan Automation", "oneLiner": "Nine-deliverable automation architecture for a non-profit lender, designed under a hard no-PII constraint.", "stack": ["FastAPI", "AI agents", "DocuSign (Phase 2)"], "status": "Designed, not built", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/cfsf-loan-automation.md" },
    { "name": "Content Pipeline", "oneLiner": "Near-fully automated Spanish video pipeline with data-driven niche validation and a tiered model-cost strategy.", "stack": ["Python", "SQLite", "Streamlit", "multi-model video APIs"], "status": "Production", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/content-pipeline.md" },
    { "name": "Anota", "oneLiner": "Native Android note app — my first mobile project, built to find out where AI assistance stops working.", "stack": ["Kotlin", "Gradle"], "status": "In development", "url": "https://github.com/jachaveza26/portfolio/blob/main/projects/anota.md" }
  ],
  "skills": {
    "hard": [
      { "group": "Languages", "items": ["Python", "TypeScript", "SQL / PLpgSQL", "Kotlin", "DAX", "R"] },
      { "group": "AI / LLM", "items": ["Claude API & tool use", "Gemini", "OpenRouter", "multi-agent orchestration", "guardrail design", "fact-checking", "prompt versioning", "Retell", "VAPI", "ElevenLabs"] },
      { "group": "Backend", "items": ["FastAPI", "Supabase (Postgres, auth, pg_cron)", "Next.js API routes", "n8n"] },
      { "group": "Frontend", "items": ["Next.js", "React", "Vite", "Tailwind"] },
      { "group": "Data / BI", "items": ["Power BI", "DAX Studio", "Tabular ALM Toolkit", "Power Query", "pandas", "Apify", "Excel"] },
      { "group": "APIs & integrations", "items": ["Google Business Profile API", "Microsoft Graph", "Meta Graph (Standard Access)", "WhatsApp Cloud API", "BuildingConnected", "Jobber GraphQL", "Zoho", "Stripe", "Twilio", "DataForSEO"] },
      { "group": "Infra", "items": ["Docker", "Caddy", "Railway", "Cloudflare Pages", "GitHub Actions"] },
      { "group": "Quality", "items": ["Vitest", "conventional commits", "sliced delivery"] },
      { "group": "Security", "items": ["OAuth 2.0", "single-use HMAC tokens", "per-tenant ownership validation"] },
      { "group": "Business tools", "items": ["Pipedrive", "HubSpot", "ActiveCampaign", "Jira", "Trello", "WordPress", "Google Analytics"] }
    ],
    "soft": [
      { "name": "Client discovery and scoping", "evidence": "Runs paid discovery workshops credited against the build, so scope is defined before code is written." },
      { "name": "Pricing and proposals", "evidence": "Prices and structures engagements — implementation fee plus retainer — writes the proposals and negotiates bounded concessions." },
      { "name": "End-to-end ownership", "evidence": "Discovery, architecture, build, deploy, billing and the on-call pager, usually as the only engineer on the project." },
      { "name": "Honest status reporting", "evidence": "Every case study ends with a status line, flattering or not; roadmap is labelled as roadmap in every deck." },
      { "name": "Communicating to different audiences", "evidence": "Presents the same analysis twice a week: dense detail to the commercial team on Monday, one message per slide to executives on Thursday." },
      { "name": "Bilingual delivery", "evidence": "Spanish with teams in Mexico, English with clients in Canada and the US; half the projects were scoped in one language and delivered in the other." },
      { "name": "Stakeholder engagement", "evidence": "Maps automation proposals to what each stakeholder actually does, not to their title." },
      { "name": "Attention to detail", "evidence": "Root-caused a company-wide BI outage to one duplicated name in a lookup table." },
      { "name": "Training and enablement", "evidence": "Ran Power BI training for an analytics team and CRM onboarding for staff at two companies." }
    ]
  },
  "education": [
    { "credential": "Master's Degree in Big Data and Omnichannel", "institution": "Esden Business School", "country": "Spain", "year": "2020" },
    { "credential": "Bachelor's Degree in Computer Science", "institution": "Instituto Tecnológico de Hermosillo", "country": "Mexico", "year": "2010" }
  ],
  "certifications": [
    { "credential": "Business Analyst Certificate", "institution": "Camosun College", "country": "Canada", "completed": "2025-03-18" },
    { "credential": "Google Data Analytics Certificate", "institution": "Esden Business School", "country": "Spain", "completed": "2020" }
  ],
  "languages": [
    { "language": "Spanish", "level": "Native" },
    { "language": "English", "level": "Fluent" }
  ]
}
```

- [ ] **Step 8: Add a schema test for the real content and run it**

Append to `test/validate.test.mjs`:

```js
import { readFileSync } from "node:fs";

test("content/cv.json validates against content/cv.schema.json", () => {
  const cv = JSON.parse(readFileSync(new URL("../content/cv.json", import.meta.url), "utf8"));
  const schema = JSON.parse(readFileSync(new URL("../content/cv.schema.json", import.meta.url), "utf8"));
  assert.deepEqual(validate(cv, schema), []);
});
```

Run: `npm test`
Expected: 5 passing. If the content test fails, the message names the exact path; fix the JSON, not the schema.

- [ ] **Step 9: Commit**

```bash
git add package.json package-lock.json content/ src/validate.mjs test/validate.test.mjs
git commit -m "feat: content schema, validator and cv.json"
```

---

### Task 2: Renderer and build

**Files:**
- Create: `src/render.mjs`, `src/build.mjs`, `test/build.test.mjs`

**Interfaces:**
- Consumes: `validate` from Task 1, `content/cv.json` shape from Task 1.
- Produces: `render(cv, { updated }) → string` (full HTML document); `esc(s)`, `fmtMonth("YYYY-MM" | null)`, `slug(s)`; `build({ outDir, now }) → outDir` writing `index.html`, `styles.css`, `assets/<photo>`, `assets/og-image.png` (if present).
- Element ids the tests and CSS rely on: sections `#about`, `#experience`, `#projects`, `#skills`, `#education`; each job `#job-<slug(org)>`; nav links `href="#about"` etc.; PDF link `href="Andres-Chavez-CV.pdf"` with class `btn-pdf`.

- [ ] **Step 1: Write the failing build tests**

`test/build.test.mjs`:

```js
import { test, before } from "node:test";
import assert from "node:assert/strict";
import { mkdtempSync, readFileSync, existsSync } from "node:fs";
import { tmpdir } from "node:os";
import path from "node:path";
import { build } from "../src/build.mjs";
import { fmtMonth, esc } from "../src/render.mjs";

let out, html;
before(() => {
  out = build({ outDir: mkdtempSync(path.join(tmpdir(), "cv-")), now: new Date("2026-09-10T00:00:00Z") });
  html = readFileSync(path.join(out, "index.html"), "utf8");
});

test("fmtMonth renders months and Present", () => {
  assert.equal(fmtMonth("2025-01"), "Jan 2025");
  assert.equal(fmtMonth("2013-05"), "May 2013");
  assert.equal(fmtMonth(null), "Present");
});

test("esc escapes html", () => {
  assert.equal(esc(`<a href="x">&'</a>`), "&lt;a href=&quot;x&quot;&gt;&amp;&#39;&lt;/a&gt;");
});

test("build writes index.html, styles.css and the photo", () => {
  assert.ok(existsSync(path.join(out, "index.html")));
  assert.ok(existsSync(path.join(out, "styles.css")));
  assert.ok(existsSync(path.join(out, "assets", "andres-chavez-400.webp")));
});

test("every in-page anchor points to an existing id", () => {
  const ids = new Set([...html.matchAll(/\sid="([^"]+)"/g)].map((m) => m[1]));
  const anchors = [...html.matchAll(/href="#([^"]+)"/g)].map((m) => m[1]);
  assert.ok(anchors.length >= 5, "expected nav anchors");
  for (const a of anchors) assert.ok(ids.has(a), `missing id for #${a}`);
});

test("every project card links to a case study in the portfolio repo", () => {
  const cards = [...html.matchAll(/class="card"[^>]*href="([^"]+)"/g)].map((m) => m[1]);
  assert.equal(cards.length, 11);
  for (const u of cards) assert.match(u, /^https:\/\/github\.com\/jachaveza26\/portfolio\/blob\/main\/projects\/[a-z0-9-]+\.md$/);
});

test("experience is newest first and the open role says Present", () => {
  const starts = [...html.matchAll(/<time datetime="(\d{4}-\d{2})"/g)].map((m) => m[1]);
  const sorted = [...starts].sort().reverse();
  assert.deepEqual(starts, sorted);
  assert.match(html, /Jan 2025<\/time> – Present/);
});

test("no phone number anywhere in the output", () => {
  assert.doesNotMatch(html, /\(\d{3}\)\s?\d{3}-\d{4}/);
  assert.doesNotMatch(html, /\b\d{3}[-.\s]\d{3}[-.\s]\d{4}\b/);
});

test("metadata: title, canonical, og image, JSON-LD person, updated date", () => {
  assert.match(html, /<title>Andrés Chávez — AI &amp; Automation Engineer<\/title>/);
  assert.match(html, /<link rel="canonical" href="https:\/\/andres\.streamlite\.ca\/">/);
  assert.match(html, /property="og:image" content="https:\/\/andres\.streamlite\.ca\/assets\/og-image\.png"/);
  assert.match(html, /"@type":"Person"/);
  assert.match(html, /Last updated 2026-09-10/);
});

test("PDF button links to the generated file", () => {
  assert.match(html, /class="btn btn-pdf" href="Andres-Chavez-CV\.pdf" download/);
});
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `node --test test/build.test.mjs`
Expected: FAIL, `Cannot find module '../src/build.mjs'`.

- [ ] **Step 3: Write `src/render.mjs`**

```js
const MONTHS = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];

export const esc = (s) =>
  String(s).replace(/[&<>"']/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[c]);

export function fmtMonth(ym) {
  if (!ym) return "Present";
  const [y, m] = ym.split("-").map(Number);
  return `${MONTHS[m - 1]} ${y}`;
}

export const slug = (s) => String(s).toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");

const li = (items) => items.map((i) => `<li>${esc(i)}</li>`).join("");

function head(cv, updated) {
  const { name, title, tagline, site, photo, email, linkedin, github, location } = cv.identity;
  const person = {
    "@context": "https://schema.org",
    "@type": "Person",
    name,
    jobTitle: title,
    email: `mailto:${email}`,
    url: `${site}/`,
    image: `${site}/assets/${photo}`,
    address: { "@type": "PostalAddress", addressLocality: "Chilliwack", addressRegion: "BC", addressCountry: "CA" },
    sameAs: [linkedin, github],
  };
  const desc = `${title} in ${location}. ${tagline}`;
  return `<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>${esc(name)} — ${esc(title)}</title>
<meta name="description" content="${esc(desc)}">
<link rel="canonical" href="${site}/">
<meta property="og:type" content="profile">
<meta property="og:title" content="${esc(name)} — ${esc(title)}">
<meta property="og:description" content="${esc(desc)}">
<meta property="og:url" content="${site}/">
<meta property="og:image" content="${site}/assets/og-image.png">
<meta name="twitter:card" content="summary_large_image">
<link rel="stylesheet" href="styles.css">
<script type="application/ld+json">${JSON.stringify(person).replace(/</g, "\\u003c")}</script>`;
}

function header(cv) {
  const { name, title, tagline, location, email, linkedin, github, photo } = cv.identity;
  return `<header class="hero">
  <img class="photo" src="assets/${esc(photo)}" alt="Portrait of ${esc(name)}" width="200" height="267">
  <div class="hero-text">
    <h1>${esc(name)}</h1>
    <p class="title">${esc(title)}</p>
    <p class="tagline">${esc(tagline)}</p>
    <p class="location">${esc(location)}</p>
    <p class="actions">
      <a class="btn" href="mailto:${esc(email)}">Email</a>
      <a class="btn" href="${esc(linkedin)}" rel="me">LinkedIn</a>
      <a class="btn" href="${esc(github)}" rel="me">GitHub</a>
      <a class="btn btn-pdf" href="Andres-Chavez-CV.pdf" download>Download PDF</a>
    </p>
    <p class="print-contact">${esc(email)} · ${esc(linkedin)} · ${esc(github)}</p>
  </div>
</header>`;
}

const nav = () => `<nav class="topnav" aria-label="Sections">
  <a href="#about">About</a><a href="#experience">Experience</a><a href="#projects">Projects</a><a href="#skills">Skills</a><a href="#education">Education</a>
</nav>`;

const about = (cv) => `<section id="about"><h2>About</h2>${cv.profile.map((p) => `<p>${esc(p)}</p>`).join("")}</section>`;

function job(j) {
  const dates = `<p class="dates"><time datetime="${j.start}">${fmtMonth(j.start)}</time> – ${j.end ? `<time datetime="${j.end}">${fmtMonth(j.end)}</time>` : "Present"}</p>`;
  const head = `<div class="job-head"><h3>${esc(j.role)}</h3><p class="org">${esc(j.org)} · ${esc(j.location)}</p>${dates}</div>`;
  if (j.compact) return `<article class="job compact" id="job-${slug(j.org)}">${head}<p class="summary">${esc(j.summary)}</p></article>`;
  const summary = j.summary ? `<p class="summary">${esc(j.summary)}</p>` : "";
  const bullets = j.bullets.length ? `<ul>${li(j.bullets)}</ul>` : "";
  const links = j.links.length
    ? `<p class="links">Case studies: ${j.links.map((l) => `<a href="${esc(l.url)}">${esc(l.label)}</a>`).join(" · ")}</p>`
    : "";
  return `<article class="job" id="job-${slug(j.org)}">${head}${summary}${bullets}${links}</article>`;
}

const experience = (cv) =>
  `<section id="experience"><h2>Experience</h2>${[...cv.experience].sort((a, b) => (a.start < b.start ? 1 : a.start > b.start ? -1 : 0)).map(job).join("")}</section>`;

const projects = (cv) => `<section id="projects"><h2>Selected projects</h2>
<p class="lede">One case study per project, written for another engineer, in the <a href="${esc(cv.identity.portfolio)}">portfolio repository</a>.</p>
<div class="grid">${cv.projects
  .map(
    (p) => `<a class="card" href="${esc(p.url)}"><h3>${esc(p.name)}</h3><p>${esc(p.oneLiner)}</p><ul class="chips">${li(p.stack)}</ul><span class="status">${esc(p.status)}</span></a>`,
  )
  .join("")}</div></section>`;

const skills = (cv) => `<section id="skills"><h2>Skills</h2>
<h3>Technical</h3>
<dl class="hard">${cv.skills.hard.map((g) => `<dt>${esc(g.group)}</dt><dd>${g.items.map(esc).join(" · ")}</dd>`).join("")}</dl>
<h3>How I work</h3>
<dl class="soft">${cv.skills.soft.map((s) => `<dt>${esc(s.name)}</dt><dd>${esc(s.evidence)}</dd>`).join("")}</dl></section>`;

const education = (cv) => `<section id="education"><h2>Education</h2>
<ul class="edu">${cv.education.map((e) => `<li><strong>${esc(e.credential)}</strong> — ${esc(e.institution)}, ${esc(e.country)}, ${esc(e.year)}</li>`).join("")}</ul>
<h3>Certifications</h3>
<ul class="edu">${cv.certifications.map((c) => `<li><strong>${esc(c.credential)}</strong> — ${esc(c.institution)}, ${esc(c.country)}, ${esc(c.completed.length === 4 ? c.completed : fmtMonth(c.completed.slice(0, 7)))}</li>`).join("")}</ul>
<h3>Languages</h3>
<p>${cv.languages.map((l) => `${esc(l.language)} (${esc(l.level)})`).join(" · ")}</p></section>`;

const footer = (cv, updated) => `<footer>
  <p><a href="mailto:${esc(cv.identity.email)}">${esc(cv.identity.email)}</a> · <a href="${esc(cv.identity.linkedin)}">LinkedIn</a> · <a href="${esc(cv.identity.github)}">GitHub</a> · <a href="${esc(cv.identity.portfolio)}">Portfolio</a></p>
  <p class="updated">Last updated ${esc(updated)}</p>
</footer>`;

export function render(cv, { updated }) {
  return `<!doctype html>
<html lang="en">
<head>
${head(cv, updated)}
</head>
<body>
${nav()}
<main>
${header(cv)}
${about(cv)}
${experience(cv)}
${projects(cv)}
${skills(cv)}
${education(cv)}
</main>
${footer(cv, updated)}
</body>
</html>
`;
}
```

- [ ] **Step 4: Write `src/build.mjs`**

```js
import { readFileSync, writeFileSync, mkdirSync, cpSync, rmSync, existsSync } from "node:fs";
import { fileURLToPath } from "node:url";
import path from "node:path";
import { validate } from "./validate.mjs";
import { render } from "./render.mjs";

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");

export function build({ outDir = path.join(root, "dist"), now = new Date() } = {}) {
  const cv = JSON.parse(readFileSync(path.join(root, "content/cv.json"), "utf8"));
  const schema = JSON.parse(readFileSync(path.join(root, "content/cv.schema.json"), "utf8"));
  const errors = validate(cv, schema);
  if (errors.length) throw new Error(`content/cv.json is invalid:\n  ${errors.join("\n  ")}`);

  rmSync(outDir, { recursive: true, force: true });
  mkdirSync(path.join(outDir, "assets"), { recursive: true });
  writeFileSync(path.join(outDir, "index.html"), render(cv, { updated: now.toISOString().slice(0, 10) }));
  cpSync(path.join(root, "src/styles.css"), path.join(outDir, "styles.css"));
  for (const file of [cv.identity.photo, "og-image.png"]) {
    const src = path.join(root, "assets", file);
    if (existsSync(src)) cpSync(src, path.join(outDir, "assets", file));
  }
  return outDir;
}

if (process.argv[1] && path.resolve(process.argv[1]) === fileURLToPath(import.meta.url)) {
  try {
    console.log(`built ${build()}`);
  } catch (err) {
    console.error(err.message);
    process.exit(1);
  }
}
```

- [ ] **Step 5: Create a placeholder stylesheet and the photo so the build has something to copy**

`src/styles.css` gets its real content in Task 3. For now:

```css
/* filled in Task 3 */
```

Photo (real, kept as-is in Task 3):

```bash
mkdir -p assets
cp "/Users/achavez/GitHub/streamlite-website/public/home/assets/andres-chavez.jpg" assets/andres-chavez.jpg
cwebp -q 82 -resize 400 0 assets/andres-chavez.jpg -o assets/andres-chavez-400.webp
```

Expected: `assets/andres-chavez-400.webp` around 15–30 KB, 400×533.

- [ ] **Step 6: Run the tests to verify they pass**

Run: `npm test`
Expected: all build tests and validator tests pass (5 + 9). If `experience is newest first` fails, check that `cv.json` order was not changed; the renderer sorts anyway.

- [ ] **Step 7: Build for real and look at the HTML**

Run: `npm run build && wc -c dist/index.html && head -30 dist/index.html`
Expected: `dist/index.html` exists, starts with `<!doctype html>`, title present.

- [ ] **Step 8: Commit**

```bash
git add src/render.mjs src/build.mjs src/styles.css assets/ test/build.test.mjs
git commit -m "feat: render cv.json to a static page"
```

---

### Task 3: Stylesheet, dark mode, print layout, Open Graph image

**Files:**
- Modify: `src/styles.css`
- Create: `scripts/og.mjs`, `assets/og-image.png`

**Interfaces:**
- Consumes: class names and ids from Task 2 (`topnav`, `hero`, `photo`, `hero-text`, `title`, `tagline`, `location`, `actions`, `btn`, `btn-pdf`, `print-contact`, `job`, `compact`, `job-head`, `org`, `dates`, `summary`, `links`, `lede`, `grid`, `card`, `chips`, `status`, `hard`, `soft`, `edu`, `updated`).
- Produces: `assets/og-image.png` 1200×630.

- [ ] **Step 1: Write `src/styles.css`**

```css
:root {
  color-scheme: light dark;
  --bg: #fbfaf7;
  --fg: #1c1b19;
  --muted: #5f5b54;
  --line: #e3dfd6;
  --accent: #0b5fa5;
  --card: #ffffff;
  --chip: #efece5;
  --max: 56rem;
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
  font-size: 17px;
  line-height: 1.55;
}
@media (prefers-color-scheme: dark) {
  :root { --bg: #15161a; --fg: #ece9e2; --muted: #a39e94; --line: #2b2d33; --accent: #6db3ee; --card: #1d1f25; --chip: #262932; }
}
* { box-sizing: border-box; }
html { scroll-behavior: smooth; scroll-padding-top: 3.5rem; }
body { margin: 0; background: var(--bg); color: var(--fg); -webkit-font-smoothing: antialiased; }
a { color: var(--accent); text-decoration-thickness: 1px; text-underline-offset: 2px; }
h1, h2, h3 { line-height: 1.2; letter-spacing: -0.01em; margin: 0; }
h1 { font-size: clamp(2rem, 5vw, 2.75rem); font-weight: 700; }
h2 { font-size: 1.35rem; font-weight: 650; margin: 0 0 1rem; padding-top: 3rem; }
h3 { font-size: 1.05rem; font-weight: 650; }
p { margin: 0 0 0.75rem; }
ul { margin: 0.25rem 0 0.75rem; padding-left: 1.2rem; }
li { margin: 0.2rem 0; }

.topnav { position: sticky; top: 0; z-index: 2; display: flex; gap: 1.25rem; padding: 0.7rem 1.25rem; background: color-mix(in srgb, var(--bg) 88%, transparent); backdrop-filter: blur(8px); border-bottom: 1px solid var(--line); font-size: 0.92rem; }
.topnav a { color: var(--muted); text-decoration: none; }
.topnav a:hover { color: var(--fg); }

main { max-width: var(--max); margin: 0 auto; padding: 2.5rem 1.25rem 4rem; }

.hero { display: grid; grid-template-columns: 1fr; gap: 1.5rem; align-items: start; padding-bottom: 1rem; border-bottom: 1px solid var(--line); }
.photo { width: 140px; height: auto; border-radius: 12px; display: block; }
.title { font-size: 1.2rem; color: var(--muted); margin: 0.35rem 0 0.75rem; }
.tagline { font-size: 1.1rem; max-width: 40rem; }
.location { color: var(--muted); }
.actions { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-top: 1rem; }
.btn { display: inline-block; padding: 0.5rem 0.9rem; border: 1px solid var(--line); border-radius: 999px; text-decoration: none; color: var(--fg); background: var(--card); font-size: 0.92rem; }
.btn:hover { border-color: var(--accent); }
.btn-pdf { background: var(--accent); color: #fff; border-color: var(--accent); }
.print-contact { display: none; }
@media (min-width: 640px) { .hero { grid-template-columns: 160px 1fr; } .photo { width: 160px; } }

.job { padding: 1.25rem 0; border-bottom: 1px solid var(--line); }
.job:last-child { border-bottom: 0; }
.job-head { display: grid; grid-template-columns: 1fr auto; gap: 0.25rem 1rem; align-items: baseline; }
.job-head h3 { grid-column: 1; }
.org { grid-column: 1; color: var(--muted); margin: 0; }
.dates { grid-column: 2; grid-row: 1 / span 2; color: var(--muted); font-variant-numeric: tabular-nums; white-space: nowrap; margin: 0; font-size: 0.92rem; }
.summary { margin-top: 0.6rem; }
.job.compact .summary { color: var(--muted); font-size: 0.95rem; }
.links { font-size: 0.92rem; color: var(--muted); }
@media (max-width: 480px) { .job-head { grid-template-columns: 1fr; } .dates { grid-column: 1; grid-row: auto; } }

.lede { color: var(--muted); margin-bottom: 1.25rem; }
.grid { display: grid; grid-template-columns: 1fr; gap: 0.9rem; }
@media (min-width: 640px) { .grid { grid-template-columns: repeat(2, 1fr); } }
.card { display: flex; flex-direction: column; gap: 0.5rem; padding: 1rem 1.1rem; background: var(--card); border: 1px solid var(--line); border-radius: 12px; color: inherit; text-decoration: none; }
.card:hover { border-color: var(--accent); }
.card h3 { color: var(--accent); }
.card p { margin: 0; font-size: 0.95rem; }
.chips { list-style: none; display: flex; flex-wrap: wrap; gap: 0.35rem; margin: 0.25rem 0 0; padding: 0; }
.chips li { font-size: 0.78rem; padding: 0.15rem 0.55rem; background: var(--chip); border-radius: 999px; margin: 0; }
.status { font-size: 0.8rem; color: var(--muted); margin-top: auto; }

dl { display: grid; grid-template-columns: 1fr; gap: 0.35rem 1.25rem; margin: 0.5rem 0 1.5rem; }
dt { font-weight: 650; }
dd { margin: 0 0 0.5rem; color: var(--muted); }
@media (min-width: 640px) { dl { grid-template-columns: 11rem 1fr; } dd { margin-bottom: 0; } }
.edu { list-style: none; padding: 0; }

footer { border-top: 1px solid var(--line); padding: 1.5rem 1.25rem 3rem; text-align: center; color: var(--muted); font-size: 0.92rem; }
.updated { margin: 0; }

@media print {
  @page { size: Letter; margin: 0.6in 0.7in; }
  :root { --bg: #fff; --fg: #111; --muted: #444; --line: #ccc; --accent: #0b4f8a; --card: #fff; --chip: #eee; font-size: 10.5pt; line-height: 1.35; }
  .topnav, footer, .actions { display: none !important; }
  main { max-width: none; padding: 0; }
  .hero { grid-template-columns: 1in 1fr; gap: 0.3in; border-bottom: 1px solid var(--line); padding-bottom: 0.15in; }
  .photo { width: 1in; border-radius: 6px; }
  .print-contact { display: block; font-size: 9.5pt; color: var(--muted); }
  h1 { font-size: 20pt; }
  h2 { font-size: 13pt; padding-top: 0.22in; margin-bottom: 0.08in; break-after: avoid; }
  h3 { font-size: 10.5pt; }
  .job { padding: 0.1in 0; break-inside: avoid; }
  .card { break-inside: avoid; padding: 0.1in; border-radius: 4px; }
  .grid { grid-template-columns: repeat(2, 1fr); gap: 0.1in; }
  .chips li { background: none; padding: 0; }
  .chips li::after { content: " ·"; }
  .chips li:last-child::after { content: ""; }
  a { color: inherit; text-decoration: none; }
  .print-contact, .links a { color: var(--accent); }
}
```

- [ ] **Step 2: Rebuild and preview in the browser, light and dark, mobile and desktop**

Run: `npm run build && npx --yes serve -l 4173 dist`
Then open `http://localhost:4173` (Browser pane, `preview_start` with a `launch.json` entry `{ "name": "cv", "runtimeExecutable": "npx", "runtimeArgs": ["--yes", "serve", "-l", "4173", "dist"], "port": 4173 }`). Check: nav sticks; hero shows photo left of text at ≥640px and stacked below; project grid is 2 columns desktop / 1 column mobile; `resize_window` with `colorScheme: "dark"` shows the dark palette with readable contrast; no horizontal scroll at 375px.

Fix anything that looks wrong in `styles.css` only. Stop the server afterwards.

- [ ] **Step 3: Write `scripts/og.mjs` and generate the Open Graph image**

Install the browser first: `npx playwright install chromium`.

`scripts/og.mjs`:

```js
import { chromium } from "playwright";
import { readFileSync } from "node:fs";
import path from "node:path";

const cv = JSON.parse(readFileSync("content/cv.json", "utf8"));
const photo = readFileSync(path.join("assets", cv.identity.photo)).toString("base64");
const html = `<!doctype html><html><head><meta charset="utf-8"><style>
  body{margin:0;width:1200px;height:630px;display:flex;align-items:center;gap:56px;padding:0 96px;box-sizing:border-box;
       background:#fbfaf7;color:#1c1b19;font-family:ui-sans-serif,system-ui,-apple-system,"Segoe UI",Roboto,Arial,sans-serif}
  img{width:300px;height:400px;object-fit:cover;border-radius:20px}
  h1{font-size:64px;margin:0 0 12px;letter-spacing:-0.02em} p{font-size:30px;margin:0;color:#5f5b54;line-height:1.3}
  .site{margin-top:28px;font-size:24px;color:#0b5fa5}
</style></head><body>
  <img src="data:image/webp;base64,${photo}" alt="">
  <div><h1>${cv.identity.name}</h1><p>${cv.identity.title}</p><p>${cv.identity.location}</p><p class="site">${cv.identity.site.replace("https://", "")}</p></div>
</body></html>`;

const browser = await chromium.launch();
const page = await browser.newPage({ viewport: { width: 1200, height: 630 }, deviceScaleFactor: 1 });
await page.setContent(html, { waitUntil: "load" });
await page.screenshot({ path: "assets/og-image.png", type: "png" });
await browser.close();
console.log("wrote assets/og-image.png");
```

Run: `npm run og && sips -g pixelWidth -g pixelHeight assets/og-image.png`
Expected: `1200` × `630`. Open the PNG (Read tool) and confirm the photo and text render.

- [ ] **Step 4: Rebuild, run the tests, commit**

Run: `npm run build && npm test`
Expected: all passing; `dist/assets/og-image.png` now exists.

```bash
git add src/styles.css scripts/og.mjs assets/og-image.png
git commit -m "feat: stylesheet with dark mode and print layout, Open Graph image"
```

---

### Task 4: PDF generation

**Files:**
- Create: `scripts/pdf.mjs`
- Modify: `src/styles.css` (only if the PDF exceeds three pages or breaks badly)

**Interfaces:**
- Consumes: `dist/index.html` from `npm run build`.
- Produces: `dist/Andres-Chavez-CV.pdf`; exits 1 if the file is missing, has 0 pages, or has more than 3 pages.

- [ ] **Step 1: Write `scripts/pdf.mjs`**

```js
import { chromium } from "playwright";
import { readFileSync, statSync, existsSync } from "node:fs";
import path from "node:path";
import { pathToFileURL } from "node:url";

const dist = path.resolve("dist");
const index = path.join(dist, "index.html");
const out = path.join(dist, "Andres-Chavez-CV.pdf");
if (!existsSync(index)) {
  console.error("dist/index.html not found — run `npm run build` first");
  process.exit(1);
}

const browser = await chromium.launch();
const page = await browser.newPage();
await page.goto(pathToFileURL(index).href, { waitUntil: "load" });
await page.emulateMedia({ media: "print" });
await page.pdf({
  path: out,
  format: "Letter",
  printBackground: true,
  preferCSSPageSize: true,
  margin: { top: "0.6in", bottom: "0.6in", left: "0.7in", right: "0.7in" },
});
await browser.close();

// Chromium writes page objects uncompressed, so counting "/Type /Page" (not "/Pages") is reliable enough for a guard.
const pages = (readFileSync(out, "latin1").match(/\/Type\s*\/Page(?!s)/g) ?? []).length;
console.log(`wrote ${out} (${statSync(out).size} bytes, ${pages} pages)`);
if (pages === 0 || pages > 3) {
  console.error(`PDF page count out of range (expected 1–3, got ${pages})`);
  process.exit(1);
}
```

- [ ] **Step 2: Generate the PDF and inspect it**

Run: `npm run build && npm run pdf`
Expected: `wrote .../dist/Andres-Chavez-CV.pdf (... bytes, 2 pages)` or 3 pages. Open `dist/Andres-Chavez-CV.pdf` with the Read tool and check: no nav, small photo, contact line with email/LinkedIn/GitHub under the title, no job split awkwardly across pages, projects in two columns.

If it is 3 pages and looks sparse, tighten print sizes in `styles.css` (font-size 10pt, `.job` padding 0.06in) and rerun. If it is more than 3 pages the script fails: reduce print font size before anything else. Do not remove content.

- [ ] **Step 3: Confirm the failure path**

Run: `rm -rf dist && npm run pdf; echo "exit=$?"`
Expected: `dist/index.html not found — run \`npm run build\` first`, `exit=1`.

- [ ] **Step 4: Commit**

```bash
git add scripts/pdf.mjs src/styles.css
git commit -m "feat: generate the CV PDF from the page with Playwright"
```

---

### Task 5: Docker image for Railway

**Files:**
- Create: `Dockerfile`, `Caddyfile`, `railway.json`, `.dockerignore`

**Interfaces:**
- Consumes: `npm run build`, `npm test`, `scripts/pdf.mjs`.
- Produces: an image that serves `dist/` on `$PORT` (default 8080) with the PDF as an attachment.

- [ ] **Step 1: Write `.dockerignore`**

```text
node_modules
dist
.git
docs
*.md
!README.md
```

- [ ] **Step 2: Write `Dockerfile`**

```dockerfile
# syntax=docker/dockerfile:1
# Stage 1: build the site and the PDF. The Playwright image ships Chromium and its libs;
# the tag MUST match the playwright version in package.json.
FROM mcr.microsoft.com/playwright:v1.63.0-jammy AS build
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
RUN node src/build.mjs && node --test test/ && node scripts/pdf.mjs

# Stage 2: serve static files.
FROM caddy:2-alpine
COPY --from=build /app/dist /srv
COPY Caddyfile /etc/caddy/Caddyfile
ENV PORT=8080
EXPOSE 8080
```

- [ ] **Step 3: Write `Caddyfile`**

```text
{
	admin off
	auto_https off
}

:{$PORT:8080} {
	root * /srv
	encode zstd gzip

	@pdf path /Andres-Chavez-CV.pdf
	header @pdf Content-Disposition "attachment; filename=\"Andres-Chavez-CV.pdf\""
	header @pdf Cache-Control "public, max-age=300"

	@static path /assets/* /styles.css
	header @static Cache-Control "public, max-age=86400"

	header X-Content-Type-Options nosniff
	header Referrer-Policy strict-origin-when-cross-origin

	file_server
}
```

- [ ] **Step 4: Write `railway.json`**

```json
{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile" },
  "deploy": { "healthcheckPath": "/", "healthcheckTimeout": 60, "restartPolicyType": "ON_FAILURE", "restartPolicyMaxRetries": 3 }
}
```

- [ ] **Step 5: Build and run the image locally**

Run:

```bash
docker build -t andres-cv . && docker run --rm -d -p 8080:8080 -e PORT=8080 --name andres-cv andres-cv
sleep 2
curl -sS -o /dev/null -w "index %{http_code} %{content_type}\n" http://localhost:8080/
curl -sS -o /dev/null -w "pdf %{http_code} %{content_type}\n" -D - http://localhost:8080/Andres-Chavez-CV.pdf | grep -iE '^(HTTP|content-disposition|content-type)|^pdf'
curl -sS -o /dev/null -w "css %{http_code}\n" http://localhost:8080/styles.css
curl -sS -o /dev/null -w "photo %{http_code}\n" http://localhost:8080/assets/andres-chavez-400.webp
docker stop andres-cv
```

Expected: `index 200 text/html; charset=utf-8`, PDF `200`, `Content-Disposition: attachment; filename="Andres-Chavez-CV.pdf"`, `css 200`, `photo 200`. The first `docker build` takes a few minutes (Playwright image download); the test stage inside the build must pass too.

- [ ] **Step 6: Commit**

```bash
git add Dockerfile Caddyfile railway.json .dockerignore
git commit -m "chore: Docker image with Caddy for Railway"
```

---

### Task 6: GitHub Actions quality gate and README

**Files:**
- Create: `.github/workflows/checks.yml`, `.htmlvalidate.json`, `README.md`

**Interfaces:**
- Consumes: `npm run build`, `npm test`, `npm run pdf`, `npm run validate:html`.
- Produces: workflow named `checks`; badge URL `https://github.com/jachaveza26/cv/actions/workflows/checks.yml/badge.svg`.

- [ ] **Step 1: Write `.htmlvalidate.json` and run html-validate locally**

```json
{
  "extends": ["html-validate:recommended"],
  "rules": {
    "no-inline-style": "off"
  }
}
```

Run: `npm run build && npm run validate:html`
Expected: `0 errors`. If it reports something (for example a heading-level rule or `wcag/h30`), fix `src/render.mjs`, rerun `npm test`, and keep the config as is unless the rule is clearly not applicable; if a rule is disabled, add a one-line comment in this task's commit message saying why.

- [ ] **Step 2: Write `.github/workflows/checks.yml`**

```yaml
name: checks

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: Build, test, validate, PDF
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Build site
        run: npm run build

      - name: Unit tests
        run: npm test

      - name: Validate HTML
        run: npm run validate:html

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Generate PDF
        run: npm run pdf

      - name: Check links
        uses: lycheeverse/lychee-action@v2
        with:
          args: --no-progress --verbose --accept 200,206,403,429,999 --base dist dist/index.html
          fail: true

      - uses: actions/upload-artifact@v4
        with:
          name: Andres-Chavez-CV
          path: dist/Andres-Chavez-CV.pdf
```

The PDF step runs before lychee on purpose: the page links to `Andres-Chavez-CV.pdf`, and lychee resolves relative links against `--base dist`.

- [ ] **Step 3: Write `README.md`**

```markdown
[![checks](https://github.com/jachaveza26/cv/actions/workflows/checks.yml/badge.svg)](https://github.com/jachaveza26/cv/actions/workflows/checks.yml)

# andres.streamlite.ca

Personal CV site for Andrés Chávez. One JSON file in, one static page and one PDF out.

- Content: `content/cv.json`, validated against `content/cv.schema.json`.
- Build: `node src/build.mjs` → `dist/` (no dependencies beyond Node 22).
- PDF: `node scripts/pdf.mjs` prints the page with headless Chromium (Playwright).
- Deploy: Railway builds the `Dockerfile` (Playwright stage builds, Caddy serves).
- Case studies live in [jachaveza26/portfolio](https://github.com/jachaveza26/portfolio); this site links to them.

## Develop

```bash
npm install
npm run build && npx serve dist
npm test
npx playwright install chromium && npm run pdf
docker build -t andres-cv . && docker run --rm -p 8080:8080 andres-cv
```
```

- [ ] **Step 4: Run the whole local pipeline once more and commit**

Run: `npm run build && npm test && npm run validate:html && npm run pdf`
Expected: all green.

```bash
git add .github/workflows/checks.yml .htmlvalidate.json README.md
git commit -m "ci: checks workflow and README"
```

---

### Task 7: Content approval, publish, Railway, DNS

This task is the only one that creates anything outside the laptop. Every external step waits for Andrés.

**Files:**
- Modify: `content/cv.json` (only if Andrés asks for wording changes)

- [ ] **Step 1: Present `content/cv.json` to Andrés for line-by-line approval**

Show him the file grouped by section (identity, profile, the Streamlite bullets, the seven CV jobs, soft skills). Apply the wording changes he asks for, rerun `npm test`, commit as `docs(content): wording changes from review`. Do not continue until he says the content is approved.

- [ ] **Step 2: Propose the publish commands and wait for approval**

```bash
gh repo create jachaveza26/cv --public --description "Personal CV site — andres.streamlite.ca" --disable-wiki
```

```bash
cd /Users/achavez/GitHub/portfolio-jachaveza26/cv && git remote add origin https://github.com/jachaveza26/cv.git && git push -u origin main
```

Before pushing, run `git ls-files` and confirm the list contains no `Resume*.pdf`, no `COMO-PUBLICAR.md`, no `dist/`, no `node_modules/`.

- [ ] **Step 3: After the push, watch the Actions run**

Run: `gh run list --limit 1` then `gh run watch <id> --exit-status`.
Expected: success. If lychee fails, read the log with `gh run view <id> --log | grep -E 'Check links.*(✗|Errors|Total)'`, report the exact link, and propose the fix; do not apply without approval.

- [ ] **Step 4: Railway service (Andrés, in the Railway dashboard; can be assisted through the browser pane)**

1. New service → Deploy from GitHub repo → `jachaveza26/cv`, branch `main`. Railway picks up `railway.json` and builds the Dockerfile.
2. Wait for the first deploy; open the generated `*.up.railway.app` URL and confirm the page and `/Andres-Chavez-CV.pdf` respond.
3. Service → Settings → Networking → Custom Domain → `andres.streamlite.ca`. Copy the CNAME target Railway shows.

- [ ] **Step 5: DNS (Andrés, in GoDaddy)**

Add `CNAME` record: name `andres`, value = the target from Step 4, TTL default. Verify from the laptop:

```bash
dig +short CNAME andres.streamlite.ca
curl -sS -o /dev/null -w "%{http_code} %{ssl_verify_result}\n" https://andres.streamlite.ca/
```

Expected: the CNAME resolves to the Railway target and the site answers `200 0` once Railway reports the certificate as issued (can take a few minutes after DNS propagates).

- [ ] **Step 6: Final verification and memory**

- Open `https://andres.streamlite.ca` in the browser pane, light and dark, mobile and desktop; download the PDF.
- Check `https://andres.streamlite.ca/Andres-Chavez-CV.pdf` returns `Content-Disposition: attachment`.
- Save a `reference` memory with the site URL, the Railway service name and the repo, and update the spec status line to "deployed".

```bash
git commit -am "docs(spec): mark as deployed"
git push origin main
```

---

## Self-review against the spec

- **Goal, language, positioning, name, contact, photo, gap, Camosun:** Task 1 content.
- **Repository layout:** Tasks 1–6 create every file listed; `docs/superpowers/specs` already exists.
- **Content model and validation:** Task 1 schema + validator; build fails on invalid content (Task 2 `build.mjs`).
- **Page structure (nav, header, about, experience, projects, skills, education, footer):** Task 2 renderer; `compact` entries for LANIX and DSC in content.
- **Visual direction (system font, light/dark, mobile-first, contrast, semantic HTML):** Task 3 stylesheet; semantic tags in Task 2.
- **Print / PDF (Letter, hidden nav/footer, small photo, visible key URLs, ≤3 pages guard, download button):** Tasks 3 and 4; `download` attribute and Caddy attachment header in Tasks 2 and 5.
- **SEO and metadata:** Task 2 `head()`; OG image Task 3.
- **Build and deploy (Actions gate, Dockerfile two stages, Caddy headers, healthcheck):** Tasks 5 and 6.
- **Quality (six listed tests, html-validate, lychee, badge):** Tasks 2 and 6.
- **Error handling:** build throws on invalid content (Task 2), PDF script exits 1 (Task 4), Docker build fails if any of those fail (Task 5), lychee fails the workflow (Task 6).
- **DNS and Railway setup, git rules:** Task 7.
- **Out of scope:** nothing in the plan renders case studies, adds forms, Spanish, analytics or routing.

Type/name consistency checked: `build({ outDir, now })`, `render(cv, { updated })`, `fmtMonth`, `esc`, `slug`, class `btn-pdf`, file `Andres-Chavez-CV.pdf`, photo `andres-chavez-400.webp`, ids `#about … #education` are used with the same names in Tasks 2, 3, 4, 5 and 6.
